# Data README

This folder contains **synthetic** examples to exercise the prototype.

- `sample_requests.csv`: toy FOIA requests and (optional) target component labels.
  Columns: `request_id`, `text`, `label` (label may be empty).

**Real data**: explore FOIA.gov and agency annual FOIA reports. Do **not** place
sensitive content in this repo.